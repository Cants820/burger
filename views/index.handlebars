<center>
<h1>Eat-Da-Burgers</h1>
</center>

<div class="container">

    <div class="row">

      <div class="col-md-6 col-xs-6">
        

        <ul>
          {{#each burger}}
            {{#unless devoured}}
            <button class="burger-btn" id={{this.id}} data-state={{this.devoured}}>{{this.burger_name}}<img alt="burger" src="assets/img/burger.jpg" width="100px" height="100px"></button>
            {{/unless}}
          {{/each}}
        </ul>
      </div>

      <div class="col-md-6 col-xs-6">
        

        <ul>
          {{#each burger}}
            {{#if devoured}}
              <button class="delete" id={{this.id}} data-state={{this.devoured}}>{{this.burger_name}}<img alt="devoured burger" src="assets/img/devoured.jpg" width="100px" height="100px"></button>
            {{/if}}
          {{/each}}
        </ul>
      </div>

    </div>

 
    <div class="row">
      <div class="col-md-4 col-xs-4"></div>
          <div class="col-md-4 col-xs-4">
            <form class="create-burger">{{!-- listens to the form --}}
            <h2>Add a Burger</h2>
                <div class="form-group">
                  <br>
                  <label for="ca">Burger Name:</label><br>
                  <textarea id ="add-burger"  rows="4" cols="25">
                    
                  </textarea>
                  <br>
                 <button id="add">Add Burger</button>
                </div>

            </form>
          </div>
      <div class="col-md-4 col-xs-4"></div>
    </div>

</div> {{!-- end container --}}

  {{!-- <button type="submit" id="devour">Devour it!!</button> --}}

